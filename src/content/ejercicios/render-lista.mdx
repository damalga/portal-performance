---
ejercicio: "Ejercicio 1"
orden: 1
titulo: "Insertar en el DOM dentro de un bucle"
categoria: dom
resumen: "Meter 500 nodos uno a uno provoca 500 recálculos. Cómo bajar a uno."
conceptos: ["reflow", "batching", "DocumentFragment"]
quiz:
  - pregunta: "¿Por qué es lento insertar 500 nodos con appendChild dentro de un bucle?"
    opciones:
      - "Porque el bucle for es más lento que forEach o map en los navegadores modernos."
      - "Porque createElement reserva memoria y el garbage collector se dispara por cada nodo insertado."
      - "Porque appendChild es asíncrono y el navegador espera que resuelva cada promesa antes de continuar."
      - "Porque cada modificación al DOM invalida el layout y puede provocar un reflow del árbol de renderizado."
      - "Porque textContent con concatenación de strings tiene coste cuadrático a partir de cierto tamaño."
    correcta: 3
  - pregunta: "¿Qué hace un DocumentFragment y por qué ayuda aquí?"
    opciones:
      - "Es una caché de estilos que el navegador comparte entre nodos del mismo tipo, evitando recalcularlos cuando se insertan varios."
      - "Es un objeto que registra automáticamente los cambios al DOM para deshacerlos si algo falla durante la inserción."
      - "Es un modo de rendering paralelo que descarga el trabajo al compositor thread para no bloquear el hilo principal."
      - "Es una promesa que resuelve cuando el navegador termina de pintar y notifica al script que puede seguir mutando."
      - "Es un contenedor invisible en memoria que puede acumular nodos y volcarlos al DOM en una única inserción."
    correcta: 4
  - pregunta: "Al hacer `container.append(...items)` con 500 elementos, ¿cuántos reflows fuerza?"
    opciones:
      - "500, uno por elemento del spread."
      - "Cero: append no fuerza reflow, solo lo marca como necesario."
      - "Uno solo: es una única operación desde el punto de vista del layout."
      - "Depende del navegador y no se puede predecir de antemano."
      - "Dos: uno al principio y otro al final del batch."
    correcta: 2
  - pregunta: "¿Cuál de estas variantes también sirve para bajar los 500 reflows a uno?"
    opciones:
      - "Concatenar strings en una variable y asignarla a `container.innerHTML` al final."
      - "Envolver el bucle en `queueMicrotask` para que el navegador batch por su cuenta."
      - "Cambiar `appendChild` por `prepend` en cada iteración."
      - "Añadir `container.style.display = 'none'` antes del bucle y devolvérselo al final."
      - "Convertir el bucle en `for...of` en vez de `for` clásico."
    correcta: 0
  - pregunta: "¿Qué es exactamente un “reflow”?"
    opciones:
      - "El recolector de basura ejecutándose sobre nodos huérfanos que ya no cuelgan del árbol del documento."
      - "El proceso por el que el navegador recalcula posiciones y tamaños de los elementos afectados por un cambio."
      - "Una llamada a la GPU para componer las capas visibles antes de mostrarlas en pantalla."
      - "El repintado de píxeles cuando cambia un color o una imagen, sin necesidad de recalcular la geometría del árbol."
      - "Un evento que el DOM emite cuando termina de dibujar y las mutaciones pendientes están ya en pantalla."
    correcta: 1
---

import Solucion from "../../components/Solucion.astro";
import DemoRenderLista from "../../components/demos/DemoRenderLista.astro";

## El problema

Necesitas pintar una lista de 500 elementos. Escribes lo obvio: un bucle
con `createElement` y `appendChild`. Funciona, pero se nota lento en móvil,
y el navegador marca la tarea como problemática en el perfilador.

El motivo no es el bucle. Es que cada `appendChild` es una modificación al
árbol del DOM, y el navegador tiene que estar preparado en cualquier
momento para responder a preguntas sobre layout (`offsetHeight`,
`getBoundingClientRect`). Aunque en la práctica intenta agrupar reflows,
un patrón de 500 mutaciones seguidas es exactamente lo que le impide hacerlo.

## El código "roto"

```javascript
const container = document.getElementById("list");

for (let i = 0; i < 500; i++) {
  const item = document.createElement("div");
  item.textContent = "Elemento " + i;
  container.appendChild(item);
}
```

## Las pistas

- Los nodos no tienen por qué vivir en el DOM mientras los construyes.
- Buscas una única operación al final del bucle, no 500.
- `DocumentFragment` existe exactamente para esto.

<Solucion>

```javascript
const container = document.getElementById("list");
const fragment = document.createDocumentFragment();

for (let i = 0; i < 500; i++) {
  const item = document.createElement("div");
  item.textContent = "Elemento " + i;
  fragment.appendChild(item);
}

container.appendChild(fragment);
```

Un `DocumentFragment` es un contenedor ligero que no forma parte del
documento visible. Puedes montar dentro toda la lista sin que el navegador
reflowee nada, y cuando insertas el fragmento en el contenedor real, el
DOM ve una sola inserción, no 500.

También sirve un array + spread, que expresa la misma idea de otra manera:

```javascript
const container = document.getElementById("list");
const items = [];

for (let i = 0; i < 500; i++) {
  const item = document.createElement("div");
  item.textContent = "Elemento " + i;
  items.push(item);
}

container.append(...items);
```

El `DocumentFragment` es el patrón canónico de la plataforma; el array +
spread lee bien y no necesita conocerlo. Cualquiera vale, siempre que se
pase de N mutaciones a una.

</Solucion>

<DemoRenderLista />
