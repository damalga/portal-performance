---
titulo: "Scroll infinito"
orden: 8
categoria: eventos
resumen: "Un listener de scroll, un fetch por disparo, y un innerHTML que reparsea todo. Tres problemas, una solución con IntersectionObserver."
conceptos: ["IntersectionObserver", "rootMargin", "condición de carrera", "insertAdjacentHTML"]
quiz:
  - pregunta: "¿Qué ventaja tiene IntersectionObserver frente a un listener de scroll para detectar si un centinela está en pantalla?"
    opciones:
      - "El navegador optimiza la detección internamente y avisa solo cuando cambia el estado de visibilidad, evitando cientos de disparos por segundo."
      - "Es más rápido porque usa un `Worker` aparte y no bloquea nunca al hilo principal del navegador."
      - "Funciona en más navegadores que el evento `scroll`, especialmente en versiones antiguas de Safari y Chrome."
      - "Permite escuchar cambios en varios elementos con una sola llamada a `addEventListener`, en lugar de una por elemento."
      - "Es un API síncrono, mientras que el evento `scroll` es asíncrono y por eso tarda en llegar al handler."
    correcta: 0
  - pregunta: "¿Para qué sirve `rootMargin` en IntersectionObserver?"
    opciones:
      - "Para forzar un margen CSS al elemento observado, empujando visualmente los elementos vecinos que lo rodean."
      - "Para limitar cuántas veces el observer puede llegar a dispararse a lo largo de la vida de la página."
      - "Para hacer que el observer ignore los cambios de tamaño de la ventana del navegador al redimensionar."
      - "Para controlar el `z-index` del elemento observado respecto a los elementos que se solapan con él."
      - "Para ampliar (o reducir) el área efectiva del viewport para la detección, disparando el observer antes de que el centinela sea visible."
    correcta: 4
  - pregunta: "¿Por qué hace falta la bandera `cargando` aunque uses IntersectionObserver?"
    opciones:
      - "No hace falta en absoluto: IntersectionObserver ya evita disparos concurrentes de forma automática por diseño."
      - "Porque IntersectionObserver no funciona correctamente en Safari sin una bandera explícita que gate los disparos."
      - "Porque mientras la primera petición está en vuelo el usuario puede seguir haciendo scroll y el observer disparar de nuevo, lanzando peticiones duplicadas para la misma página."
      - "Para forzar al navegador a pausar el scroll del usuario mientras dura el fetch de la página siguiente."
      - "Para que la variable `siguientePagina` se incremente correctamente sin saltarse ningún número intermedio."
    correcta: 2
  - pregunta: "¿Por qué bajar la bandera `cargando` en `.finally()` y no en `.then()`?"
    opciones:
      - "Porque `.then` no acepta funciones que modifiquen variables externas al scope de la cadena de promesas."
      - "Porque `.finally` se ejecuta antes que `.then` en la cadena y por eso llega a tiempo de bajar la bandera."
      - "Porque `.finally` cachea internamente el resultado de la petición y `.then` en cambio no lo hace."
      - "Porque si la petición falla y solo la bajas en `.then`, la bandera queda a `true` y el scroll infinito se rompe: nunca vuelve a cargar."
      - "Porque `.then` solo funciona con promesas resueltas específicamente por `fetch` y no por otras APIs."
    correcta: 3
  - pregunta: '¿Por qué `insertAdjacentHTML("beforeend", ...)` es mejor que `innerHTML += ...` para añadir contenido al final?'
    opciones:
      - "Porque `insertAdjacentHTML` es un API más nuevo y por eso está mejor optimizado en general por todos los motores."
      - "Porque `insertAdjacentHTML` parsea solo el fragmento nuevo, mientras que `+=` serializa todo el contenido existente, lo concatena y lo reparsea entero."
      - "Porque `+=` no funciona correctamente con etiquetas semánticas de HTML5 como `<article>` o `<section>`."
      - "Porque `insertAdjacentHTML` sanitiza automáticamente el HTML contra XSS antes de insertarlo en el DOM."
      - "Porque `+=` provoca layout thrashing en cada uso, mientras que `insertAdjacentHTML` lo evita por diseño."
    correcta: 1
---

import Solucion from "../../components/Solucion.astro";

## El problema

Scroll infinito de artículos. El código roto es un compendio: un
listener de scroll que se dispara sin parar, un fetch por disparo (sin
bandera que evite duplicados), y un `innerHTML +=` que reparsea la
lista entera cada vez que llega una página nueva.

Los tres problemas se resuelven con tres piezas distintas, y merece
la pena verlos por separado.

## El código roto

```javascript
const lista = document.getElementById("articulos");
const centinela = document.getElementById("cargar-mas");
let siguientePagina = 2;

window.addEventListener("scroll", () => {
  const rect = centinela.getBoundingClientRect();

  if (rect.top < window.innerHeight) {
    fetch("/api/articulos?pagina=" + siguientePagina)
      .then((res) => res.json())
      .then((articulos) => {
        lista.innerHTML += articulos.map((a) => `<article>${a.titulo}</article>`).join("");
        siguientePagina++;
      });
  }
});
```

## Las pistas

- `IntersectionObserver` avisa cuando un elemento cambia de estado de visibilidad; no necesitas escuchar scroll para saber si el centinela está cerca.
- Una bandera `cargando` que se levanta al empezar el fetch y se baja al terminar evita peticiones duplicadas.
- `insertAdjacentHTML("beforeend", ...)` inserta al final sin reparsear lo que ya hay.

<Solucion>

```javascript
const lista = document.getElementById("articulos");
const centinela = document.getElementById("cargar-mas");
let siguientePagina = 2;
let cargando = false;

const observer = new IntersectionObserver(
  ([ele]) => {
    if (!ele.isIntersecting) return;
    if (cargando) return;

    cargando = true;

    fetch("/api/articulos?pagina=" + siguientePagina)
      .then((res) => res.json())
      .then((articulos) => {
        lista.insertAdjacentHTML("beforeend", articulos.map((a) => `<article>${a.titulo}</article>`).join(""));
        siguientePagina++;
      })
      .finally(() => {
        cargando = false;
      });
  },
  { rootMargin: "300px" },
);

observer.observe(centinela);
```

Tres piezas:

**IntersectionObserver** con `rootMargin: "300px"` observa un elemento
centinela colocado al final de la lista. En vez de escuchar `scroll`
(que se dispara sin parar), el observer avisa solo cuando el estado de
visibilidad del centinela cambia. El `rootMargin` expande el área
efectiva del viewport 300px por debajo, así que empezamos a cargar la
página siguiente antes de que el usuario llegue al fondo — cuando llega,
los artículos ya están.

**Bandera `cargando`**. IntersectionObserver reduce los disparos, pero
no los elimina: si el usuario hace scroll con el centinela en pantalla,
el observer puede volver a dispararse mientras la primera petición
todavía está en vuelo. La bandera bloquea peticiones concurrentes para
la misma página. Se sube al empezar, se baja en `.finally()`. En
`.finally` y no en `.then` porque si la petición falla (red caída,
500 del servidor) y la bandera queda a `true`, el scroll infinito
se rompe para siempre. `.finally` cubre éxito y fallo, que es lo que
quieres.

**`insertAdjacentHTML("beforeend", ...)`** añade el HTML nuevo al
final del contenedor sin tocar lo que ya hay. `innerHTML += ...` es
peor de lo que parece: para calcular el nuevo valor, el navegador
serializa el contenido actual del nodo, lo concatena con el nuevo
trozo, y parsea el resultado entero de vuelta. En una lista con 100
artículos, cargar la página 6 supone re-parsear los 500 anteriores
para pintar los 5 nuevos. `insertAdjacentHTML` parsea solo el
fragmento nuevo.

Este es el script que gobierna el scroll infinito real de la home.

</Solucion>
